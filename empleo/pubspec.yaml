name: empleo
description: Ofretas de empleo en Cantabria.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 3.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  http: ^0.12.2
  html: ^0.14.0+3
  provider: ^4.3.1
  flutter_launcher_icons: ^0.7.5

flutter_icons:
  image_path: 'assets/icon/icon.png'
  android: true
  ios: true

    # flutter pub pub run flutter_launcher_icons:main

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:


  uses-material-design: true


